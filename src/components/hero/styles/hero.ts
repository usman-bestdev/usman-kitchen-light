import styles from "./hero.module.css";

export const mainContainerClasses = `${styles.mainContainer}`;

export const leftBackgroundClasses = `${styles.leftBackground}`;

export const leftContainerClasses = `${styles.leftContainer}`;

export const dishImageClasses = `${styles.dishImage}`;

export const headingClasses = `h1 primaryFont`;

export const descriptionClasses = `secondaryColor pb-2 pt-3`;

export const buttonContainerClasses = `d-flex gap-3 align-items-center`;

export const buttonClasses = `text-white text-uppercase primaryButton p-3 `;

export const mainRowClasses = `${styles.rowContainer} m-0`;

export const rightContainerClasses = `d-flex justify-content-end align-item-center p-0`;
