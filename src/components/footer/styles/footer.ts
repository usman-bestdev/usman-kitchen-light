import styles from "./footer.module.css";

export const mainContainerClasses = `${styles.mainContainer} `;

export const mainBackgroundClasses = `${styles.background}`;

export const logoContainerClasses = `${styles.logoContainer}`;

export const listGroupItemClasses = `${styles.customListGroupItem}`;

export const footerBottomClasses = `${styles.footerBottom}`;
