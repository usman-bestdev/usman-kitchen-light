import styles from "./bestFood.module.css";

export const mainContainerClasses = `${styles.mainContainer} `;

export const mainBackgroundClasses = `${styles.background}`;

export const columnClasses = `${styles.columnStyle}`;

export const dishImageContainerClasses = `${styles.dishImageContainer}`;

export const dishImageClasses = `${styles.dishImage}`;

export const itemContainerClasses = `d-flex flex-column justify-content-center p-0`;

export const itemHeadingClasses = `h3 d-flex primaryFont`;

export const itemDescriptionClasses = `secondaryColor`;

export const itemReviewClasses = `secondaryColor fw-400`;

export const itemImageClasses = `${styles.foodItemImage}`;
