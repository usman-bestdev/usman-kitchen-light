import styles from "./category.module.css";

export const mainContainerClasses = `${styles.mainContainer}`;

export const mainBackgroundClasses = `${styles.background}`;

export const cardImageClasses = `${styles.cardImage}`;

export const mainRowClasses = `d-flex align-items-center p-3 `;

export const subHeadingClasses = `text-white px-3 text-uppercase text-center fw-600`;

export const headingClasses = `h2 primaryFont text-white text-center`;

export const buttonContainerClasses = `d-flex justify-content-center m-3`;
